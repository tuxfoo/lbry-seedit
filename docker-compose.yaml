version: '3.4'

services:
  lbrynet:
    container_name: seedit
    build:
      context: ./seedit
    ports:
      - "4444:4444"
      - 50001:50001
      - 5289:5280
    volumes:
      - ./seedit/core/lbrynet_home:/home/lbrynet